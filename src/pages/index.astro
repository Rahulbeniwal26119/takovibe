---
import Layout from "../layouts/Layout.astro";
import BlogCard from "../components/BlogCard.astro";

// API configuration
const API_URL = import.meta.env.PUBLIC_API_URL || 'http://localhost:8000';

// Interface for API blog post
interface ApiBlogPost {
  id: number;
  created_at: string;
  updated_at: string;
  author: {
    id: number;
    name: string;
    email: string;
    username: string;
  };
  title: string;
  uri: string;
  slug: string;
  tags: string[];
  description: string;
  image_url: string;
  is_published: boolean;
}

// Fetch blog posts and tags from API
let blogEntries: ApiBlogPost[] = [];
let publishedPosts: ApiBlogPost[] = [];
let sortedPosts: ApiBlogPost[] = [];
let featuredPosts: ApiBlogPost[] = [];
let tagsData: { [key: string]: number } = {};
let popularTags: Array<{ name: string; count: number }> = [];

try {
  // Fetch blog posts
  const blogsResponse = await fetch(`${API_URL}/api/blogs/home/home-blogs/`);
  if (blogsResponse.ok) {
    const blogsResult = await blogsResponse.json();
    if (blogsResult.code === 0 && blogsResult.data) {
      blogEntries = blogsResult.data;

      // Filter out unpublished posts
      publishedPosts = blogEntries.filter(post => post.is_published);

      // Sort by created_at date (newest first)
      sortedPosts = publishedPosts.sort(
        (a, b) => new Date(b.created_at).getTime() - new Date(a.created_at).getTime()
      );

      // Get featured posts (top 6)
      featuredPosts = sortedPosts.slice(0, 6);
    }
  }

  // Fetch tags statistics
  const tagsResponse = await fetch(`${API_URL}/api/blogs/tags/`);
  if (tagsResponse.ok) {
    const tagsResult = await tagsResponse.json();
    if (tagsResult.code === 0 && tagsResult.data) {
      tagsData = tagsResult.data;

      // Convert tags object to array and sort by count (most popular first)
      popularTags = Object.entries(tagsData)
        .map(([name, count]) => ({ name, count }))
        .sort((a, b) => b.count - a.count)
        .slice(0, 8); // Get top 8 tags
    }
  }
} catch (error) {
  console.error("Error fetching data from API:", error);
  // Keep empty arrays as fallback
}
---

<Layout
  title="TakoVibe - Modern Tech Platform for Developers"
  description="Join TakoVibe, a collaborative platform where developers and tech writers share insights on software engineering, system design, and modern technologies."
  type="website"
  author="TakoVibe Team"
  tags={["software engineering", "system design", "technology", "programming", "web development", "tech community"]}
>
  <div class="min-h-screen bg-white dark:bg-[#171923]">

    <!-- Hero Section -->
    <section class="pb-20 px-4 sm:px-6" id="about">
      <div class="max-w-5xl mx-auto">
        <div class="grid lg:grid-cols-12 gap-8 lg:gap-12 items-center">
          <!-- Main Content -->
          <div class="lg:col-span-12 order-1">
            <div class="text-center">
              <div class="inline-flex items-center gap-2 px-4 py-2 bg-gray-100 dark:bg-gray-800 text-gray-800 dark:text-gray-200 rounded-full text-sm font-medium mb-8 border border-gray-200 dark:border-gray-700 mt-20">
                <span class="text-base">💎</span>
                From Developers to Developers
              </div>
              <h1 class="text-4xl sm:text-5xl lg:text-6xl font-light text-gray-900 dark:text-gray-100 mb-6 sm:mb-8 leading-tight">
                <span class="bg-gradient-to-r from-purple-600 via-purple-500 to-blue-600 bg-clip-text text-transparent font-semibold">
                  TakoVibe
                </span>
                <br/>
                <span class="text-2xl sm:text-3xl lg:text-4xl font-normal text-gray-700 dark:text-gray-300">
                  AI News + Tech Education Hub
                </span>
              </h1>
              <p class="text-lg sm:text-xl text-gray-700 dark:text-gray-300 mb-8 leading-relaxed max-w-3xl mx-auto font-normal">
                Your complete tech intelligence platform: 
                <span class="text-emerald-700 dark:text-emerald-300 font-semibold">breaking AI news</span> 
                paired with 
                <span class="text-blue-700 dark:text-blue-300 font-semibold">deep technical education</span>. 
                Stay informed, keep learning.
              </p>
              
              <!-- Enhanced Dual Value Proposition -->
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 max-w-3xl mx-auto mb-8">
                <div class="flex items-center gap-4 px-6 py-5 bg-gradient-to-r from-emerald-500/10 to-cyan-500/10 border border-emerald-200 dark:border-emerald-700 rounded-xl hover:shadow-lg hover:shadow-emerald-500/10 transition-all duration-300 transform hover:scale-105 group">
                  <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-gradient-to-r from-emerald-500 to-cyan-500 rounded-xl flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform duration-300">
                      <span class="text-2xl">�</span>
                    </div>
                  </div>
                  <div class="text-left">
                    <div class="font-bold text-emerald-700 dark:text-emerald-300 text-base">Breaking AI News</div>
                    <div class="text-sm text-gray-600 dark:text-gray-400">Daily updates • Industry trends • Real-time alerts</div>
                  </div>
                </div>
                <div class="flex items-center gap-4 px-6 py-5 bg-gradient-to-r from-blue-500/10 to-purple-500/10 border border-blue-200 dark:border-blue-700 rounded-xl hover:shadow-lg hover:shadow-blue-500/10 transition-all duration-300 transform hover:scale-105 group">
                  <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-purple-500 rounded-xl flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform duration-300">
                      <span class="text-2xl">🎓</span>
                    </div>
                  </div>
                  <div class="text-left">
                    <div class="font-bold text-blue-700 dark:text-blue-300 text-base">Expert Tech Guides</div>
                    <div class="text-sm text-gray-600 dark:text-gray-400">Tutorials • Best practices • Deep insights</div>
                  </div>
                </div>
              </div>

              <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="#content-hub" class="group inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-emerald-600 to-cyan-600 hover:from-emerald-700 hover:to-cyan-700 text-white font-semibold rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105">
                  <span class="flex items-center gap-2">
                    🚨 <span>Latest AI News</span>
                  </span>
                  <svg class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                  </svg>
                </a>
                <a href="#content-hub" class="group inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-semibold rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105">
                  <span class="flex items-center gap-2">
                    � <span>Tech Education</span>
                  </span>
                  <svg class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                  </svg>
                </a>
              </div>
            </div>
          </div>

          <!-- Stats Grid - Balanced Content Metrics -->
          <div class="lg:col-span-12 order-2 mt-16">
            <div class="grid grid-cols-2 sm:grid-cols-4 gap-4 max-w-4xl mx-auto stats-grid">
              <!-- Tech Articles Count -->
              <div class="bg-gradient-to-br from-blue-50 to-purple-50 dark:from-blue-900/20 dark:to-purple-900/20 border border-blue-200/50 dark:border-blue-400/30 rounded-xl p-6 text-center transition-all duration-300 hover:shadow-lg hover:scale-105">
                <div class="text-3xl font-bold text-gray-900 dark:text-gray-100 mb-2" id="total-blogs">
                  <div class="animate-pulse bg-gray-300 dark:bg-gray-600 h-8 w-16 mx-auto rounded"></div>
                </div>
                <div class="text-blue-600 dark:text-blue-400 text-sm font-medium">
                  Tech Articles
                </div>
              </div>

              <!-- AI News Count -->
              <div class="bg-gradient-to-br from-emerald-50 to-cyan-50 dark:from-emerald-900/20 dark:to-cyan-900/20 border border-emerald-200/50 dark:border-emerald-400/30 rounded-xl p-6 text-center transition-all duration-300 hover:shadow-lg hover:scale-105">
                <div class="text-3xl font-bold text-gray-900 dark:text-gray-100 mb-2" id="ai-news-count">
                  <div class="animate-pulse bg-gray-300 dark:bg-gray-600 h-8 w-16 mx-auto rounded"></div>
                </div>
                <div class="text-emerald-600 dark:text-emerald-400 text-sm font-medium">
                  AI News
                </div>
              </div>

              <!-- Total Categories -->
              <div class="bg-gradient-to-br from-purple-50 to-indigo-50 dark:from-purple-900/20 dark:to-indigo-900/20 border border-purple-200/50 dark:border-purple-400/30 rounded-xl p-6 text-center transition-all duration-300 hover:shadow-lg hover:scale-105">
                <div class="text-3xl font-bold text-gray-900 dark:text-gray-100 mb-2" id="total-categories">
                  <div class="animate-pulse bg-gray-300 dark:bg-gray-600 h-8 w-16 mx-auto rounded"></div>
                </div>
                <div class="text-purple-600 dark:text-purple-400 text-sm font-medium">
                  Topics
                </div>
              </div>

              <!-- Weekly Updates -->
              <div class="bg-gradient-to-br from-orange-50 to-red-50 dark:from-orange-900/20 dark:to-red-900/20 border border-orange-200/50 dark:border-orange-400/30 rounded-xl p-6 text-center transition-all duration-300 hover:shadow-lg hover:scale-105">
                <div class="text-3xl font-bold text-gray-900 dark:text-gray-100 mb-2" id="weekly-updates">
                  <div class="animate-pulse bg-gray-300 dark:bg-gray-600 h-8 w-16 mx-auto rounded"></div>
                </div>
                <div class="text-orange-600 dark:text-orange-400 text-sm font-medium">
                  Weekly Updates
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- No duplicate mobile section needed as we have responsive grid already -->
      </div>
    </section>

    <!-- Featured Series - Value Proposition Section -->
    <section class="py-12 px-4 sm:px-6 bg-gradient-to-br from-white via-purple-50/30 to-blue-50/30 dark:from-gray-900 dark:via-purple-900/10 dark:to-blue-900/10 border-t border-purple-100/50 dark:border-purple-800/30 relative overflow-hidden">
      <!-- Subtle background elements -->
      <div class="absolute top-0 left-1/3 w-64 h-64 bg-gradient-to-r from-purple-400/10 to-blue-400/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 right-1/3 w-48 h-48 bg-gradient-to-r from-blue-400/10 to-indigo-400/10 rounded-full blur-2xl"></div>

      <div class="max-w-6xl mx-auto relative z-10">
        <!-- Compact Header -->
        <div class="text-center mb-8">
          <div class="inline-flex items-center gap-2 px-3 py-1.5 bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm text-purple-700 dark:text-purple-300 rounded-full text-sm font-medium mb-3 border border-purple-200/70 dark:border-purple-700/50 shadow-sm">
            <span class="text-sm">🔥</span>
            Live Learning Paths
          </div>
          <h2 class="text-xl sm:text-2xl font-bold text-gray-900 dark:text-gray-100 mb-2">
            <span class="bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">
              Active Series
            </span>
          </h2>
          <p class="text-sm text-gray-600 dark:text-gray-400 max-w-lg mx-auto">
            Browse our ongoing learning series with new articles published regularly
          </p>
        </div>

        <!-- Series List Container -->
        <div id="featured-series-container" class="mb-6">
          <!-- Series list will be loaded here -->
        </div>

        <!-- Compact CTA -->
        <div class="text-center">
          <a href="/series" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-medium rounded-lg shadow-md hover:shadow-lg transition-all duration-300 transform hover:scale-105 text-sm group">
            <span>View All Series</span>
            <svg class="ml-1.5 w-3 h-3 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
    </section>

    <!-- Dual Content Hub - Equal Priority Section -->
    <section class="py-20 px-4 sm:px-6 border-t border-black/10 dark:border-white/10 relative overflow-hidden" id="content-hub">
      <!-- Background decorative elements -->
      <div class="absolute inset-0 bg-gradient-to-b from-purple-50/20 via-transparent to-emerald-50/20 dark:from-purple-900/10 dark:via-transparent dark:to-emerald-900/10"></div>
      <div class="absolute top-20 right-10 w-32 h-32 bg-gradient-to-r from-purple-400/10 to-blue-400/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-20 left-10 w-32 h-32 bg-gradient-to-r from-emerald-400/10 to-cyan-400/10 rounded-full blur-3xl"></div>

      <div class="max-w-7xl mx-auto relative z-10">
        <!-- Section Header -->
        <div class="text-center mb-16">
          <div class="inline-flex items-center gap-2 px-4 py-2 bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm text-gray-700 dark:text-gray-300 rounded-full text-sm font-medium mb-6 border border-gray-200/70 dark:border-gray-700/50 shadow-sm">
            <span class="text-base">⚡</span>
            Live Content Hub
          </div>
          <h2 class="text-4xl sm:text-5xl font-bold text-gray-900 dark:text-gray-100 mb-4">
            Stay <span class="bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">Informed</span> &
            <span class="bg-gradient-to-r from-emerald-600 to-cyan-600 bg-clip-text text-transparent">Learn</span>
          </h2>
          <p class="text-xl text-gray-600 dark:text-gray-400 max-w-3xl mx-auto leading-relaxed">
            Breaking AI news for instant updates, in-depth tech articles for lasting knowledge
          </p>
        </div>

        <!-- Equal Priority Content Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          
          <!-- AI News Column -->
          <div class="order-1 lg:order-1">
            <div class="flex items-center justify-between mb-8">
              <div class="flex items-center gap-4">
                <div class="w-10 h-10 bg-gradient-to-r from-emerald-500 to-cyan-500 rounded-xl flex items-center justify-center shadow-lg">
                  <span class="text-white text-lg">🚨</span>
                </div>
                <div>
                  <h3 class="text-2xl font-bold text-gray-900 dark:text-gray-100">Breaking AI News</h3>
                  <p class="text-sm text-emerald-600 dark:text-emerald-400 font-medium">Updated daily • Latest trends</p>
                </div>
              </div>
              <a href="/ai" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-emerald-500 to-cyan-500 text-white font-medium rounded-lg hover:from-emerald-600 hover:to-cyan-600 transition-all duration-300 group text-sm">
                <span>All News</span>
                <svg class="ml-1.5 w-3 h-3 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </a>
            </div>
            
            <div id="ai-news-preview" class="space-y-4 mb-6">
              <!-- AI News will be loaded here -->
              <div class="text-center py-8">
                <div class="animate-spin w-8 h-8 border-3 border-emerald-500/20 border-t-emerald-500 rounded-full mx-auto mb-3"></div>
                <p class="text-gray-600 dark:text-gray-400 text-sm">Loading latest AI news...</p>
              </div>
            </div>

            <!-- AI News Value Props -->
            <div class="grid grid-cols-2 gap-4 mt-6">
              <div class="bg-emerald-50/80 dark:bg-emerald-900/20 border border-emerald-200/50 dark:border-emerald-700/50 rounded-xl p-4 text-center">
                <div class="text-emerald-600 dark:text-emerald-400 text-2xl font-bold mb-1" id="ai-news-count-display">12+</div>
                <div class="text-emerald-700 dark:text-emerald-300 text-xs font-medium">Weekly Updates</div>
              </div>
              <div class="bg-cyan-50/80 dark:bg-cyan-900/20 border border-cyan-200/50 dark:border-cyan-700/50 rounded-xl p-4 text-center">
                <div class="text-cyan-600 dark:text-cyan-400 text-2xl font-bold mb-1">5min</div>
                <div class="text-cyan-700 dark:text-cyan-300 text-xs font-medium">Daily Read</div>
              </div>
            </div>
          </div>

          <!-- Tech Articles Column -->
          <div class="order-2 lg:order-2">
            <div class="flex items-center justify-between mb-8">
              <div class="flex items-center gap-4">
                <div class="w-10 h-10 bg-gradient-to-r from-purple-500 to-blue-500 rounded-xl flex items-center justify-center shadow-lg">
                  <span class="text-white text-lg">📚</span>
                </div>
                <div>
                  <h3 class="text-2xl font-bold text-gray-900 dark:text-gray-100">In-Depth Tech Articles</h3>
                  <p class="text-sm text-purple-600 dark:text-purple-400 font-medium">Expert insights • Practical guides</p>
                </div>
              </div>
              <a href="/blog" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-purple-500 to-blue-500 text-white font-medium rounded-lg hover:from-purple-600 hover:to-blue-600 transition-all duration-300 group text-sm">
                <span>All Articles</span>
                <svg class="ml-1.5 w-3 h-3 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </a>
            </div>

            <div id="latest-articles-preview" class="space-y-4 mb-6">
              <!-- Featured blog posts -->
              {featuredPosts.length > 0 ? (
                featuredPosts.slice(0, 3).map((post, index) => (
                  <div class="group bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl p-5 border border-gray-200/50 dark:border-gray-700/50 hover:border-purple-300 dark:hover:border-purple-500 transition-all duration-300 hover:shadow-lg hover:shadow-purple-500/10 transform hover:-translate-y-1">
                    <div class="flex gap-4">
                      <div class="flex-1">
                        <h4 class="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-2 group-hover:text-purple-600 dark:group-hover:text-purple-400 transition-colors line-clamp-2">
                          {post.title}
                        </h4>
                        <p class="text-gray-600 dark:text-gray-400 text-sm mb-3 line-clamp-2">
                          {post.description}
                        </p>
                        <div class="flex items-center gap-3 text-xs text-gray-500 dark:text-gray-500">
                          <span>📅 {new Date(post.created_at).toLocaleDateString()}</span>
                          <span>⏱️ 5 min read</span>
                          {post.tags && post.tags.length > 0 && (
                            <span class="px-2 py-1 bg-purple-100 dark:bg-purple-900/30 text-purple-700 dark:text-purple-300 rounded-md">
                              {post.tags[0]}
                            </span>
                          )}
                        </div>
                      </div>
                      {post.image_url && (
                        <div class="w-20 h-20 bg-gray-200 dark:bg-gray-700 rounded-lg overflow-hidden flex-shrink-0">
                          <img src={post.image_url} alt={post.title} class="w-full h-full object-cover" />
                        </div>
                      )}
                    </div>
                  </div>
                ))
              ) : (
                <div class="text-center py-8">
                  <div class="text-4xl mb-4">📝</div>
                  <h4 class="text-xl font-semibold text-gray-900 dark:text-gray-100 mb-2">
                    Articles Coming Soon
                  </h4>
                  <p class="text-gray-600 dark:text-gray-400">
                    In-depth tutorials and guides will appear here.
                  </p>
                </div>
              )}
            </div>

            <!-- Tech Articles Value Props -->
            <div class="grid grid-cols-2 gap-4 mt-6">
              <div class="bg-purple-50/80 dark:bg-purple-900/20 border border-purple-200/50 dark:border-purple-700/50 rounded-xl p-4 text-center">
                <div class="text-purple-600 dark:text-purple-400 text-2xl font-bold mb-1" id="total-blogs-display">{publishedPosts.length}+</div>
                <div class="text-purple-700 dark:text-purple-300 text-xs font-medium">Deep Guides</div>
              </div>
              <div class="bg-blue-50/80 dark:bg-blue-900/20 border border-blue-200/50 dark:border-blue-700/50 rounded-xl p-4 text-center">
                <div class="text-blue-600 dark:text-blue-400 text-2xl font-bold mb-1">15min</div>
                <div class="text-blue-700 dark:text-blue-300 text-xs font-medium">Avg Read</div>
              </div>
            </div>
          </div>
        </div>

        <!-- Cross-Promotion Banner -->
        <div class="bg-gradient-to-r from-purple-500/10 via-blue-500/10 to-emerald-500/10 border border-gradient-to-r border-purple-200/30 dark:border-purple-700/30 rounded-2xl p-8 text-center">
          <h3 class="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-3">
            <span class="bg-gradient-to-r from-purple-600 via-blue-600 to-emerald-600 bg-clip-text text-transparent">
              Complete Your Tech Knowledge
            </span>
          </h3>
          <p class="text-gray-600 dark:text-gray-400 mb-6 max-w-2xl mx-auto">
            Stay informed with breaking AI news, then dive deep with our comprehensive tutorials. 
            The perfect combination for modern developers.
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <button class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-emerald-600 to-cyan-600 text-white font-semibold rounded-xl hover:from-emerald-700 hover:to-cyan-700 transition-all duration-300 transform hover:scale-105 group">
              <span class="mr-2">🚨</span>
              <span>Subscribe to AI Alerts</span>
              <svg class="ml-2 w-4 h-4 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-5 5-5-5h5v-5" />
              </svg>
            </button>
            <button class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-purple-600 to-blue-600 text-white font-semibold rounded-xl hover:from-purple-700 hover:to-blue-700 transition-all duration-300 transform hover:scale-105 group">
              <span class="mr-2">📚</span>
              <span>Join Learning Path</span>
              <svg class="ml-2 w-4 h-4 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </button>
          </div>
        </div>

        <!-- Bottom decorative line -->
        <div class="mt-16 flex justify-center">
          <div class="w-32 h-1 bg-gradient-to-r from-purple-500 via-blue-500 to-emerald-500 rounded-full"></div>
        </div>
      </div>
    </section>

    <!-- Topics Showcase Section - Redesigned -->
    <section class="py-20 sm:py-28 px-4 sm:px-6 bg-gradient-to-br from-slate-50 via-blue-50/40 to-purple-50/40 dark:from-slate-900 dark:via-blue-950/40 dark:to-purple-950/40 relative overflow-hidden" id="topics">
      <!-- Advanced background elements -->
      <div class="absolute inset-0">
        <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-gradient-to-r from-blue-400/8 to-purple-400/8 rounded-full blur-3xl animate-pulse"></div>
        <div class="absolute bottom-1/4 right-1/4 w-80 h-80 bg-gradient-to-r from-purple-400/8 to-pink-400/8 rounded-full blur-3xl animate-pulse" style="animation-delay: 3s"></div>
        <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-72 h-72 bg-gradient-to-r from-indigo-400/6 to-blue-400/6 rounded-full blur-3xl animate-pulse" style="animation-delay: 1.5s"></div>
      </div>

      <div class="max-w-7xl mx-auto relative z-10">
        <!-- Enhanced Header -->
        <div class="text-center mb-16 sm:mb-20">
          <div class="inline-flex items-center gap-3 px-6 py-3 bg-white/90 dark:bg-slate-800/90 backdrop-blur-sm text-slate-700 dark:text-slate-300 rounded-full text-sm font-medium mb-8 border border-slate-200/80 dark:border-slate-700/80 shadow-sm">
            <div class="relative">
              <span class="text-lg">🎯</span>
              <div class="absolute -inset-1 bg-gradient-to-r from-blue-500/20 to-purple-500/20 rounded-full blur-sm"></div>
            </div>
            <span class="font-semibold">Explore by Topics</span>
          </div>

          <h2 class="text-5xl sm:text-6xl lg:text-7xl font-bold text-slate-900 dark:text-slate-100 mb-6 sm:mb-8 leading-tight">
            <span class="bg-gradient-to-r from-blue-600 via-purple-600 to-pink-600 bg-clip-text text-transparent">
              Popular
            </span>
            <br class="hidden sm:block" />
            <span class="text-slate-800 dark:text-slate-200 font-light">Topics</span>
          </h2>

          <p class="text-xl sm:text-2xl text-slate-600 dark:text-slate-400 max-w-4xl mx-auto leading-relaxed font-light">
            Discover articles organized by the most engaging topics in our community.
            <br class="hidden sm:block" />
            <span class="text-lg text-slate-500 dark:text-slate-500">Each topic shows the number of available articles.</span>
          </p>
        </div>

        <!-- Enhanced Topic Cards Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 sm:gap-8 mb-12">
          {popularTags.slice(0, 8).map((tag, index) => {
            const tagPosts = sortedPosts.filter(post => (post.tags || []).includes(tag.name));

            // Enhanced color schemes
            const colorSchemes = [
              {
                gradient: 'from-blue-500 to-cyan-500',
                bg: 'from-blue-50 to-cyan-50 dark:from-blue-950/30 dark:to-cyan-950/30',
                border: 'border-blue-200/60 dark:border-blue-700/60',
                text: 'text-blue-700 dark:text-blue-300',
                accent: 'bg-blue-500'
              },
              {
                gradient: 'from-purple-500 to-pink-500',
                bg: 'from-purple-50 to-pink-50 dark:from-purple-950/30 dark:to-pink-950/30',
                border: 'border-purple-200/60 dark:border-purple-700/60',
                text: 'text-purple-700 dark:text-purple-300',
                accent: 'bg-purple-500'
              },
              {
                gradient: 'from-emerald-500 to-teal-500',
                bg: 'from-emerald-50 to-teal-50 dark:from-emerald-950/30 dark:to-teal-950/30',
                border: 'border-emerald-200/60 dark:border-emerald-700/60',
                text: 'text-emerald-700 dark:text-emerald-300',
                accent: 'bg-emerald-500'
              },
              {
                gradient: 'from-orange-500 to-red-500',
                bg: 'from-orange-50 to-red-50 dark:from-orange-950/30 dark:to-red-950/30',
                border: 'border-orange-200/60 dark:border-orange-700/60',
                text: 'text-orange-700 dark:text-orange-300',
                accent: 'bg-orange-500'
              },
              {
                gradient: 'from-indigo-500 to-purple-500',
                bg: 'from-indigo-50 to-purple-50 dark:from-indigo-950/30 dark:to-purple-950/30',
                border: 'border-indigo-200/60 dark:border-indigo-700/60',
                text: 'text-indigo-700 dark:text-indigo-300',
                accent: 'bg-indigo-500'
              },
              {
                gradient: 'from-rose-500 to-pink-500',
                bg: 'from-rose-50 to-pink-50 dark:from-rose-950/30 dark:to-pink-950/30',
                border: 'border-rose-200/60 dark:border-rose-700/60',
                text: 'text-rose-700 dark:text-rose-300',
                accent: 'bg-rose-500'
              },
              {
                gradient: 'from-teal-500 to-blue-500',
                bg: 'from-teal-50 to-blue-50 dark:from-teal-950/30 dark:to-blue-950/30',
                border: 'border-teal-200/60 dark:border-teal-700/60',
                text: 'text-teal-700 dark:text-teal-300',
                accent: 'bg-teal-500'
              },
              {
                gradient: 'from-violet-500 to-purple-500',
                bg: 'from-violet-50 to-purple-50 dark:from-violet-950/30 dark:to-purple-950/30',
                border: 'border-violet-200/60 dark:border-violet-700/60',
                text: 'text-violet-700 dark:text-violet-300',
                accent: 'bg-violet-500'
              }
            ];

            // Enhanced icons mapping
            const icons = {
              'python': '🐍',
              'javascript': '⚡',
              'react': '⚛️',
              'django': '🌟',
              'database': '🗄️',
              'devops': '⚙️',
              'nginx': '🌐',
              'docker': '🐳',
              'system design': '🏗️',
              'programming': '💻',
              'ai': '🤖',
              'machine learning': '🧠',
              'web development': '🌍',
              'backend': '🔧',
              'frontend': '🎨',
              'api': '🔗',
              'microservices': '🔀',
              'cloud': '☁️',
              'security': '🔒',
              'performance': '⚡',
              'tutorial': '📚',
              'guide': '📖',
              'tips': '💡',
              'best practices': '✨'
            };

            const scheme = colorSchemes[index % colorSchemes.length];
            const tagLower = tag.name.toLowerCase();
            const icon = icons[tagLower] || '📝';

            return (
              <article
                class="group relative animate-fade-up hover:z-10"
                style={`animation-delay: ${index * 0.1}s`}
              >
                {/* Hover glow effect */}
                <div class={`absolute -inset-2 bg-gradient-to-r ${scheme.gradient} opacity-0 group-hover:opacity-20 rounded-2xl blur-xl transition-all duration-500`}></div>

                <div class={`relative bg-gradient-to-br ${scheme.bg} backdrop-blur-sm rounded-2xl p-6 border ${scheme.border} hover:border-opacity-100 transition-all duration-300 hover:shadow-2xl hover:shadow-black/5 dark:hover:shadow-white/5 transform hover:-translate-y-2 hover:scale-105 h-full flex flex-col`}>

                  {/* Header with icon and stats */}
                  <div class="flex items-start justify-between mb-4">
                    <div class={`relative p-3 rounded-xl bg-gradient-to-br ${scheme.gradient} shadow-lg`}>
                      <span class="text-2xl filter drop-shadow-sm">{icon}</span>
                      <div class={`absolute -inset-1 ${scheme.accent} opacity-20 rounded-xl blur-sm group-hover:opacity-40 transition-opacity`}></div>
                    </div>

                    <div class="text-right">
                      <div class={`text-2xl font-bold ${scheme.text} mb-1`}>
                        {tag.count}
                      </div>
                      <div class="text-xs text-slate-500 dark:text-slate-400 font-medium uppercase tracking-wider">
                        {tag.count === 1 ? 'Article' : 'Articles'}
                      </div>
                    </div>
                  </div>

                  {/* Topic name */}
                  <h3 class={`text-xl font-bold text-slate-900 dark:text-slate-100 mb-3 capitalize group-hover:${scheme.text} transition-colors duration-300 leading-tight`}>
                    {tag.name.replace(/[-_]/g, ' ')}
                  </h3>

                  {/* Description */}
                  <p class="text-slate-600 dark:text-slate-400 text-sm mb-6 leading-relaxed flex-1">
                    Comprehensive guides and tutorials covering {tag.name.replace(/[-_]/g, ' ')} development and best practices.
                  </p>

                  {/* Action button */}
                  <button
                    class={`category-modal-btn w-full inline-flex items-center justify-center px-4 py-3 bg-gradient-to-r ${scheme.gradient} text-white font-semibold rounded-xl hover:shadow-lg hover:shadow-black/20 dark:hover:shadow-white/10 transition-all duration-300 transform hover:scale-105 group/btn`}
                    data-category={tag.name}
                    data-articles={JSON.stringify(tagPosts.map(post => ({
                      title: post.title,
                      description: post.description,
                      date: post.created_at,
                      slug: post.slug,
                      tags: post.tags || [],
                      readingTime: "5 min read"
                    })))}
                  >
                    <span>Explore {tag.name}</span>
                    <svg class="ml-2 w-4 h-4 group-hover/btn:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                    </svg>
                  </button>
                </div>
              </article>
            );
          })}
        </div>

        {/* Enhanced Call-to-Action */}
        <div class="text-center">
          <div class="inline-flex flex-col sm:flex-row items-center gap-4 sm:gap-6">
            <a
              href="/categories"
              class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-semibold rounded-xl shadow-lg hover:shadow-xl hover:shadow-blue-500/25 transition-all duration-300 transform hover:scale-105 group"
            >
              <span>Browse All Categories</span>
              <svg class="ml-3 w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </a>

            <div class="text-slate-600 dark:text-slate-400 text-sm">
              <span class="font-medium">{popularTags.reduce((sum, tag) => sum + tag.count, 0)}+</span> articles across <span class="font-medium">{popularTags.length}</span> topics
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Growth & Community Impact Section -->
    <section class="py-20 px-4 sm:px-6 bg-gradient-to-br from-slate-50 via-blue-50/30 to-purple-50/30 dark:from-slate-900 dark:via-blue-950/30 dark:to-purple-950/30 relative overflow-hidden" id="impact">
      <!-- Background elements -->
      <div class="absolute inset-0">
        <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-gradient-to-r from-blue-400/8 to-purple-400/8 rounded-full blur-3xl animate-pulse"></div>
        <div class="absolute bottom-1/4 right-1/4 w-80 h-80 bg-gradient-to-r from-purple-400/8 to-emerald-400/8 rounded-full blur-3xl animate-pulse" style="animation-delay: 3s"></div>
      </div>

      <div class="max-w-7xl mx-auto relative z-10">
        <!-- Header -->
        <div class="text-center mb-16">
          <div class="inline-flex items-center gap-3 px-6 py-3 bg-white/90 dark:bg-slate-800/90 backdrop-blur-sm text-slate-700 dark:text-slate-300 rounded-full text-sm font-medium mb-8 border border-slate-200/80 dark:border-slate-700/80 shadow-sm">
            <span class="text-lg">🚀</span>
            <span class="font-semibold">Growing Tech Community</span>
          </div>

          <h2 class="text-4xl sm:text-5xl font-bold text-slate-900 dark:text-slate-100 mb-6">
            <span class="bg-gradient-to-r from-blue-600 via-purple-600 to-emerald-600 bg-clip-text text-transparent">
              Why Developers
            </span>
            <br class="hidden sm:block" />
            <span class="text-slate-800 dark:text-slate-200 font-light">Choose TakoVibe</span>
          </h2>

          <p class="text-xl text-slate-600 dark:text-slate-400 max-w-3xl mx-auto leading-relaxed">
            Join thousands of developers who rely on us for both breaking AI news and comprehensive technical education.
          </p>
        </div>

        <!-- Key Metrics & Features Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-16">
          <!-- Daily AI Updates -->
          <div class="bg-gradient-to-br from-emerald-50 to-cyan-50 dark:from-emerald-950/30 dark:to-cyan-950/30 border border-emerald-200/60 dark:border-emerald-700/60 rounded-2xl p-6 text-center hover:shadow-lg hover:shadow-emerald-500/10 transition-all duration-300 transform hover:-translate-y-1">
            <div class="w-12 h-12 bg-gradient-to-r from-emerald-500 to-cyan-500 rounded-xl flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">🚨</span>
            </div>
            <div class="text-3xl font-bold text-emerald-700 dark:text-emerald-300 mb-2">Daily</div>
            <div class="text-emerald-600 dark:text-emerald-400 font-medium text-sm">AI News Updates</div>
            <p class="text-slate-600 dark:text-slate-400 text-xs mt-2">Breaking industry news as it happens</p>
          </div>

          <!-- Deep Tech Articles -->
          <div class="bg-gradient-to-br from-purple-50 to-blue-50 dark:from-purple-950/30 dark:to-blue-950/30 border border-purple-200/60 dark:border-purple-700/60 rounded-2xl p-6 text-center hover:shadow-lg hover:shadow-purple-500/10 transition-all duration-300 transform hover:-translate-y-1">
            <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-blue-500 rounded-xl flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">📚</span>
            </div>
            <div class="text-3xl font-bold text-purple-700 dark:text-purple-300 mb-2">{publishedPosts.length}+</div>
            <div class="text-purple-600 dark:text-purple-400 font-medium text-sm">Expert Articles</div>
            <p class="text-slate-600 dark:text-slate-400 text-xs mt-2">In-depth tutorials & guides</p>
          </div>

          <!-- Topics Coverage -->
          <div class="bg-gradient-to-br from-blue-50 to-indigo-50 dark:from-blue-950/30 dark:to-indigo-950/30 border border-blue-200/60 dark:border-blue-700/60 rounded-2xl p-6 text-center hover:shadow-lg hover:shadow-blue-500/10 transition-all duration-300 transform hover:-translate-y-1">
            <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-indigo-500 rounded-xl flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">🎯</span>
            </div>
            <div class="text-3xl font-bold text-blue-700 dark:text-blue-300 mb-2">{popularTags.length}+</div>
            <div class="text-blue-600 dark:text-blue-400 font-medium text-sm">Tech Topics</div>
            <p class="text-slate-600 dark:text-slate-400 text-xs mt-2">From AI to system design</p>
          </div>

          <!-- Community Engagement -->
          <div class="bg-gradient-to-br from-orange-50 to-red-50 dark:from-orange-950/30 dark:to-red-950/30 border border-orange-200/60 dark:border-orange-700/60 rounded-2xl p-6 text-center hover:shadow-lg hover:shadow-orange-500/10 transition-all duration-300 transform hover:-translate-y-1">
            <div class="w-12 h-12 bg-gradient-to-r from-orange-500 to-red-500 rounded-xl flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">👥</span>
            </div>
            <div class="text-3xl font-bold text-orange-700 dark:text-orange-300 mb-2">5k+</div>
            <div class="text-orange-600 dark:text-orange-400 font-medium text-sm">Weekly Reads</div>
            <p class="text-slate-600 dark:text-slate-400 text-xs mt-2">Growing developer community</p>
          </div>
        </div>

        <!-- Value Proposition Features -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <!-- AI News Value -->
          <div class="group bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-2xl p-8 border border-emerald-200/50 dark:border-emerald-700/50 hover:border-emerald-300 dark:hover:border-emerald-500 transition-all duration-300 hover:shadow-lg hover:shadow-emerald-500/10 transform hover:-translate-y-1">
            <div class="w-16 h-16 bg-gradient-to-r from-emerald-500 to-cyan-500 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
              <span class="text-3xl">⚡</span>
            </div>
            <h3 class="text-xl font-bold text-slate-900 dark:text-slate-100 mb-3">Real-Time AI Intelligence</h3>
            <p class="text-slate-600 dark:text-slate-400 leading-relaxed mb-4">
              Get breaking AI news, product launches, and industry shifts delivered fresh daily. Stay ahead of the curve in the fastest-moving tech sector.
            </p>
            <ul class="space-y-2 text-sm text-slate-600 dark:text-slate-400">
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-emerald-500 rounded-full"></span>
                Daily AI industry updates
              </li>
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-emerald-500 rounded-full"></span>
                Product launch coverage
              </li>
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-emerald-500 rounded-full"></span>
                Startup & funding news
              </li>
            </ul>
          </div>

          <!-- Tech Articles Value -->
          <div class="group bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-2xl p-8 border border-purple-200/50 dark:border-purple-700/50 hover:border-purple-300 dark:hover:border-purple-500 transition-all duration-300 hover:shadow-lg hover:shadow-purple-500/10 transform hover:-translate-y-1">
            <div class="w-16 h-16 bg-gradient-to-r from-purple-500 to-blue-500 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
              <span class="text-3xl">🎓</span>
            </div>
            <h3 class="text-xl font-bold text-slate-900 dark:text-slate-100 mb-3">Deep Technical Learning</h3>
            <p class="text-slate-600 dark:text-slate-400 leading-relaxed mb-4">
              Master complex technologies with comprehensive tutorials, practical guides, and real-world examples from experienced developers.
            </p>
            <ul class="space-y-2 text-sm text-slate-600 dark:text-slate-400">
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-purple-500 rounded-full"></span>
                Step-by-step tutorials
              </li>
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-purple-500 rounded-full"></span>
                Best practices & patterns
              </li>
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-purple-500 rounded-full"></span>
                Real-world case studies
              </li>
            </ul>
          </div>

          <!-- Combined Value -->
          <div class="group bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-2xl p-8 border border-blue-200/50 dark:border-blue-700/50 hover:border-blue-300 dark:hover:border-blue-500 transition-all duration-300 hover:shadow-lg hover:shadow-blue-500/10 transform hover:-translate-y-1">
            <div class="w-16 h-16 bg-gradient-to-r from-blue-500 to-indigo-500 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
              <span class="text-3xl">🔗</span>
            </div>
            <h3 class="text-xl font-bold text-slate-900 dark:text-slate-100 mb-3">Complete Tech Ecosystem</h3>
            <p class="text-slate-600 dark:text-slate-400 leading-relaxed mb-4">
              The perfect synergy: stay informed with breaking news, then dive deep with educational content. Your one-stop tech knowledge hub.
            </p>
            <ul class="space-y-2 text-sm text-slate-600 dark:text-slate-400">
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-blue-500 rounded-full"></span>
                News to learning pipeline
              </li>
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-blue-500 rounded-full"></span>
                Cross-topic connections
              </li>
              <li class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 bg-blue-500 rounded-full"></span>
                Developer community
              </li>
            </ul>
          </div>
        </div>
      </div>
    </section>
          
          <div class="group bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-2xl p-6 border border-cyan-200/50 dark:border-cyan-700/50 hover:border-cyan-300 dark:hover:border-cyan-500 transition-all duration-300 hover:shadow-lg hover:shadow-cyan-500/10 transform hover:-translate-y-1">
            <div class="w-12 h-12 bg-gradient-to-r from-cyan-500 to-blue-500 rounded-xl flex items-center justify-center mb-4 group-hover:scale-110 transition-transform duration-300">
              <span class="text-2xl">🔗</span>
            </div>
            <h4 class="text-lg font-bold text-gray-900 dark:text-gray-100 mb-2">LLMs & RAG</h4>
            <p class="text-gray-600 dark:text-gray-400 text-sm">Large Language Models, RAG systems, and prompt engineering techniques.</p>
          </div>
          
          <div class="group bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-2xl p-6 border border-blue-200/50 dark:border-blue-700/50 hover:border-blue-300 dark:hover:border-blue-500 transition-all duration-300 hover:shadow-lg hover:shadow-blue-500/10 transform hover:-translate-y-1">
            <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-purple-500 rounded-xl flex items-center justify-center mb-4 group-hover:scale-110 transition-transform duration-300">
              <span class="text-2xl">📊</span>
            </div>
            <h4 class="text-lg font-bold text-gray-900 dark:text-gray-100 mb-2">AI Tools & Frameworks</h4>
            <p class="text-gray-600 dark:text-gray-400 text-sm">Practical guides for TensorFlow, PyTorch, Langchain, and more.</p>
          </div>
        </div>

        <!-- CTA Section -->
        <div class="text-center">
          <div class="inline-flex flex-col sm:flex-row items-center gap-4">
            <a href="/ai" class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-emerald-600 to-cyan-600 hover:from-emerald-700 hover:to-cyan-700 text-white font-semibold rounded-xl shadow-lg hover:shadow-xl hover:shadow-emerald-500/25 transition-all duration-300 transform hover:scale-105 group">
              <span class="flex items-center gap-2">
                🤖 <span>Explore AI Hub</span>
              </span>
              <svg class="ml-3 w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </a>
            
            <div class="text-gray-600 dark:text-gray-400 text-sm">
              <span class="font-medium" id="ai-content-count">25+</span> AI articles and growing daily
            </div>
          </div>
        </div>
      </div>
    </section>

  </div>

  <!-- Category Modal -->
  <div id="categoryModal" class="fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm z-50 hidden opacity-0 transition-all duration-300">
    <div class="flex items-center justify-center min-h-screen p-4">
      <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl max-w-4xl w-full max-h-[80vh] overflow-hidden transform scale-95 transition-transform duration-300" id="modalContent">
        <!-- Modal Header -->
        <div class="flex items-center justify-between p-6 border-b border-gray-200 dark:border-gray-700">
          <div class="flex items-center gap-3">
            <div id="modalIcon" class="w-10 h-10 bg-gradient-to-r from-blue-500 to-blue-600 rounded-xl flex items-center justify-center text-white text-xl">
              🐍
            </div>
            <div>
              <h2 id="modalTitle" class="text-2xl font-bold text-gray-900 dark:text-white capitalize">Python</h2>
              <p id="modalCount" class="text-gray-600 dark:text-gray-400 text-sm">16 articles</p>
            </div>
          </div>
          <button id="closeModalBtn" class="p-2 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-lg transition-colors">
            <svg class="w-6 h-6 text-gray-500 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        <!-- Search Bar -->
        <div class="p-6 border-b border-gray-200 dark:border-gray-700">
          <div class="relative">
            <input
              type="text"
              id="articleSearch"
              placeholder="Search articles..."
              class="w-full pl-10 pr-4 py-3 bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition-all"
            />
            <svg class="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
          </div>
        </div>

        <!-- Articles List -->
        <div class="max-h-96 overflow-y-auto" id="modalArticlesList">
          <!-- Articles will be populated here -->
        </div>

        <!-- Modal Footer -->
        <div class="p-6 border-t border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-700/50">
          <div class="flex items-center justify-between">
            <p class="text-gray-600 dark:text-gray-400 text-sm">
              Browse all articles or use search to find specific topics
            </p>
            <a href="/categories" class="inline-flex items-center text-indigo-600 dark:text-indigo-400 hover:text-indigo-700 dark:hover:text-indigo-300 font-medium text-sm">
              View all categories
              <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

</Layout>

<script>
  let currentArticles = [];

  // API configuration
  const API_URL = import.meta.env.PUBLIC_API_URL || 'http://localhost:8000';

  // Helper functions for series
  function getSeriesIcon(topics = []) {
    const topicLower = topics.join(' ').toLowerCase();
    if (topicLower.includes('ai') || topicLower.includes('machine learning') || topicLower.includes('rag')) return "🤖";
    if (topicLower.includes('django') || topicLower.includes('web')) return "🌐";
    if (topicLower.includes('system') || topicLower.includes('design')) return "🏗️";
    if (topicLower.includes('algorithm') || topicLower.includes('dsa')) return "🧠";
    if (topicLower.includes('python')) return "🐍";
    if (topicLower.includes('javascript') || topicLower.includes('react')) return "⚛️";
    return "📚";
  }

  function getDifficultyColor(startLevel, endLevel) {
    const levels = { beginner: 1, intermediate: 2, advanced: 3 };
    const start = levels[startLevel] || 1;
    const end = levels[endLevel] || 1;
    const avg = (start + end) / 2;

    if (avg <= 1.5) return "from-purple-500 to-blue-500";
    if (avg <= 2.5) return "from-blue-500 to-purple-500";
    return "from-orange-500 to-red-500";
  }

  function capitalize(text) {
    return text.charAt(0).toUpperCase() + text.slice(1);
  }

  function formatDate(dateString) {
    return new Date(dateString).toLocaleDateString('en-US', {
      month: 'long',
      day: 'numeric',
      year: 'numeric'
    });
  }

  // Format number for display (add + suffix for large numbers)
  function formatStatNumber(number) {
    if (number >= 1000) {
      return (number / 1000).toFixed(1).replace(/\.0$/, '') + 'K+';
    }
    return number + '+';
  }

  // Fetch and display homepage stats
  async function loadHomeStats() {
    const API_URL = import.meta.env.PUBLIC_API_URL || 'http://localhost:8000';

    try {
      const response = await fetch(`${API_URL}/api/blogs/home/our-stats/`);
      if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);

      const result = await response.json();
      const stats = result.data;

      // Update each stat with animation
      const statElements = [
        { id: 'total-blogs', value: stats.total_blogs },
        { id: 'total-categories', value: stats.total_categories },
        { id: 'total-authors', value: stats.total_authors },
        { id: 'total-views', value: stats.total_views }
      ];

      statElements.forEach(({ id, value }, index) => {
        setTimeout(() => {
          const element = document.getElementById(id);
          if (element) {
            element.innerHTML = formatStatNumber(value);
            element.style.animation = 'slideUp 0.5s ease-out';
          }
        }, index * 100); // Stagger the animations
      });

    } catch (error) {
      console.error('Failed to load stats:', error);

      // Fallback to default values on error
      const fallbackStats = [
        { id: 'total-blogs', value: '25+' },
        { id: 'total-categories', value: '10+' },
        { id: 'total-authors', value: '2+' },
        { id: 'total-views', value: '3K+' }
      ];

      fallbackStats.forEach(({ id, value }, index) => {
        setTimeout(() => {
          const element = document.getElementById(id);
          if (element) {
            element.innerHTML = value;
            element.style.animation = 'slideUp 0.5s ease-out';
          }
        }, index * 100);
      });
    }
  }

  // Fetch and display featured series
  async function loadFeaturedSeries() {
    const container = document.getElementById('featured-series-container');

    // Show loading state
    container.innerHTML = `
      <div class="col-span-full flex items-center justify-center py-8">
        <div class="flex items-center gap-2 text-gray-600 dark:text-gray-400">
          <svg class="animate-spin w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
          </svg>
          <span class="text-sm">Loading series...</span>
        </div>
      </div>
    `;

    try {
      const response = await fetch(`${API_URL}/api/blogs/series/?page_size=10`);
      if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);

      const data = await response.json();
      const allSeries = data.results || []; // Show all series, not just live ones

      if (allSeries.length === 0) {
        container.innerHTML = `
          <div class="col-span-full text-center py-8">
            <div class="text-4xl mb-3">🚀</div>
            <h3 class="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-2">
              New Series Coming Soon!
            </h3>
            <p class="text-gray-600 dark:text-gray-400 mb-4 text-sm">
              We're preparing amazing content series for you.
            </p>
            <a href="/blog" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-purple-600 to-blue-600 text-white rounded-lg font-semibold hover:shadow-lg transition-all duration-200 hover:scale-105 text-sm">
              <span>Explore Articles</span>
              <svg class="ml-2 w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        `;
        return;
      }

      // Display series as a simple list
      const displaySeries = allSeries.slice(0, 10); // Show more series in list format

      container.innerHTML = `
        <!-- Series List -->
        <div class="space-y-4">
          ${displaySeries.map((series, index) => {
            const articlesCount = series.articles_count || 0;
            const plannedArticles = series.planned_articles || 0;
            const icon = getSeriesIcon(series.topic_covered || []);

            return `
              <div class="flex items-center justify-between p-4 bg-white/95 dark:bg-gray-900/95 backdrop-blur-sm rounded-lg border border-purple-200/50 dark:border-purple-700/30 hover:border-purple-300 dark:hover:border-purple-400/50 hover:shadow-md transition-all duration-200" style="animation-delay: ${index * 0.05}s">
                <div class="flex items-center gap-4">
                  <div class="w-10 h-10 bg-gradient-to-r from-purple-600 to-blue-600 rounded-lg flex items-center justify-center text-lg flex-shrink-0">
                    ${icon}
                  </div>
                  <div class="min-w-0">
                    <div class="flex items-center gap-3 mb-1">
                      <h3 class="text-lg font-semibold text-gray-900 dark:text-gray-100">
                        ${series.title}
                      </h3>
                      <div class="flex items-center gap-1 ${series.is_live ? 'bg-purple-50 dark:bg-purple-900/30 text-purple-700 dark:text-purple-400' : 'bg-green-50 dark:bg-green-900/30 text-green-700 dark:text-green-400'} px-2 py-1 rounded-full text-xs font-medium">
                        <div class="w-1.5 h-1.5 ${series.is_live ? 'bg-purple-500 animate-pulse' : 'bg-green-500'} rounded-full"></div>
                        ${series.is_live ? 'Live' : 'Complete'}
                      </div>
                    </div>
                    <div class="flex items-center gap-3 text-sm text-gray-600 dark:text-gray-400">
                      ${series.topic_covered && series.topic_covered.length > 0 ? `
                        <span>${series.topic_covered.slice(0, 2).join(', ')}</span>
                      ` : ''}
                    </div>
                  </div>
                </div>
                <a href="/series/${series.slug}" class="text-purple-600 dark:text-purple-400 font-medium hover:underline text-sm">
                  View Series →
                </a>
              </div>
            `;
          }).join('')}
        </div>
      `;
    } catch (error) {
      console.error('Failed to load ongoing series:', error);
      container.innerHTML = `
        <div class="col-span-full text-center py-8">
          <div class="text-4xl mb-3">⚠️</div>
          <h3 class="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-2">
            Unable to Load Series
          </h3>
          <p class="text-gray-600 dark:text-gray-400 mb-4 text-sm">
            Connection issue. Please try again.
          </p>
          <button
            onclick="loadFeaturedSeries()"
            class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-purple-600 to-blue-600 text-white rounded-lg font-semibold hover:shadow-lg transition-all duration-200 hover:scale-105 text-sm"
          >
            <svg class="w-3 h-3 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
            </svg>
            Retry
          </button>
        </div>
      `;
    }
  }

  // Declare global functions
  declare global {
    interface Window {
      openCategoryModal: (category: string, articles: any[]) => void;
      closeCategoryModal: () => void;
      loadFeaturedSeries: () => Promise<void>;
      loadHomeStats: () => Promise<void>;
    }
  }

  // Load AI Content for homepage
  async function loadAIContent() {
    await Promise.all([loadAINews(), updateAICounters()]);
  }

  // Load real AI News from content collection
  async function loadAINews() {
    const container = document.getElementById('ai-news-preview');
    if (!container) return;

    try {
      // Fetch AI news - this would ideally be from an API or your content collection
      const response = await fetch('/api/ai-news.json');
      let aiNews = [];
      
      if (response.ok) {
        const data = await response.json();
        aiNews = data.slice(0, 4); // Show latest 4 news items
      } else {
        // Fallback to sample data if API is not available
        aiNews = [
          {
            title: "OpenAI Releases GPT-4 Turbo with Enhanced Vision",
            summary: "Latest model shows improved multimodal capabilities and reduced pricing for developers.",
            source: "OpenAI",
            publishedAt: new Date(Date.now() - 7200000).toISOString(), // 2 hours ago
            category: "llms",
            breaking: true,
            trending: true
          },
          {
            title: "Google's Gemini Pro Advances Multimodal AI",
            summary: "New benchmark results show competitive performance across multiple tasks and domains.",
            source: "Google DeepMind",
            publishedAt: new Date(Date.now() - 14400000).toISOString(), // 4 hours ago
            category: "research",
            featured: true
          },
          {
            title: "Meta's Code Llama 2 Transforms Programming",
            summary: "Advanced code generation capabilities designed specifically for software developers.",
            source: "Meta AI",
            publishedAt: new Date(Date.now() - 28800000).toISOString(), // 8 hours ago
            category: "releases",
            trending: true
          },
          {
            title: "AI Startup Funding Reaches $50B in Q4",
            summary: "Investment in AI companies continues to surge with record-breaking funding rounds.",
            source: "TechCrunch",
            publishedAt: new Date(Date.now() - 43200000).toISOString(), // 12 hours ago
            category: "funding"
          }
        ];
      }

      const categoryConfig = {
        'llms': { color: 'from-blue-500 to-cyan-500', icon: '🧠' },
        'research': { color: 'from-purple-500 to-pink-500', icon: '🔬' },
        'releases': { color: 'from-emerald-500 to-teal-500', icon: '🚀' },
        'funding': { color: 'from-orange-500 to-red-500', icon: '💰' },
        'startups': { color: 'from-indigo-500 to-purple-500', icon: '💡' },
        'enterprise': { color: 'from-gray-500 to-slate-500', icon: '🏢' },
        'regulation': { color: 'from-yellow-500 to-orange-500', icon: '⚖️' },
        'hardware': { color: 'from-teal-500 to-cyan-500', icon: '🔧' },
        'ethics': { color: 'from-green-500 to-emerald-500', icon: '🤝' },
        'partnerships': { color: 'from-pink-500 to-rose-500', icon: '🤝' }
      };

      function getTimeAgo(dateString) {
        const now = new Date();
        const publishedDate = new Date(dateString);
        const diffMs = now.getTime() - publishedDate.getTime();
        const diffHours = Math.floor(diffMs / (1000 * 60 * 60));
        const diffDays = Math.floor(diffHours / 24);
        
        if (diffDays > 0) {
          return `${diffDays} day${diffDays > 1 ? 's' : ''} ago`;
        } else if (diffHours > 0) {
          return `${diffHours} hour${diffHours > 1 ? 's' : ''} ago`;
        } else {
          return 'Just now';
        }
      }

      container.innerHTML = aiNews.map((news, index) => {
        const config = categoryConfig[news.category] || categoryConfig['llms'];
        const badges = [];
        
        if (news.breaking) badges.push('<span class="px-1.5 py-0.5 bg-red-500 text-white text-xs font-bold rounded uppercase tracking-wide">Breaking</span>');
        if (news.trending) badges.push('<span class="px-1.5 py-0.5 bg-orange-500 text-white text-xs font-bold rounded uppercase tracking-wide">Trending</span>');
        
        return `
          <article class="group bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-xl p-5 border border-gray-200/50 dark:border-gray-700/50 hover:border-emerald-300 dark:hover:border-emerald-500 transition-all duration-300 hover:shadow-lg hover:shadow-emerald-500/10 transform hover:-translate-y-1" style="animation-delay: ${index * 0.1}s">
            <div class="flex items-start justify-between mb-3">
              <div class="flex items-center gap-2">
                <span class="px-2 py-1 bg-gradient-to-r ${config.color} text-white text-xs font-medium rounded-md flex items-center gap-1">
                  <span>${config.icon}</span>
                  ${news.category.toUpperCase()}
                </span>
                ${badges.join('')}
              </div>
              <span class="text-xs text-gray-500 dark:text-gray-400 font-medium">${getTimeAgo(news.publishedAt)}</span>
            </div>
            
            <h4 class="text-lg font-bold text-gray-900 dark:text-gray-100 mb-2 group-hover:text-emerald-600 dark:group-hover:text-emerald-400 transition-colors duration-300 leading-tight line-clamp-2">
              ${news.title}
            </h4>
            
            <p class="text-gray-600 dark:text-gray-400 text-sm mb-4 leading-relaxed line-clamp-2">
              ${news.summary}
            </p>
            
            <div class="flex items-center justify-between">
              <span class="text-xs text-gray-500 dark:text-gray-400 font-medium flex items-center gap-1">
                <span class="w-2 h-2 bg-emerald-500 rounded-full"></span>
                ${news.source}
              </span>
              <button class="text-emerald-600 dark:text-emerald-400 hover:text-emerald-700 dark:hover:text-emerald-300 text-sm font-medium transition-colors group/btn">
                Read More
                <svg class="inline ml-1 w-3 h-3 group-hover/btn:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </button>
            </div>
          </article>
        `;
      }).join('');

    } catch (error) {
      console.error('Failed to load AI news:', error);
      container.innerHTML = `
        <div class="text-center py-8">
          <div class="text-4xl mb-3">🤖</div>
          <h4 class="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-2">AI News Coming Soon</h4>
          <p class="text-gray-600 dark:text-gray-400 text-sm">Stay tuned for the latest AI industry updates.</p>
        </div>
      `;
    }
  }

  // Update AI-related counters
  async function updateAICounters() {
    try {
      // Update AI news count display
      const aiCountDisplay = document.getElementById('ai-news-count-display');
      if (aiCountDisplay) {
        // This would typically come from your content collection count
        aiCountDisplay.textContent = '3+'; // Number of AI news articles
      }

      // Update the stats in hero section
      const aiNewsCount = document.getElementById('ai-news-count');
      if (aiNewsCount) {
        aiNewsCount.innerHTML = '<div class="text-3xl font-bold text-gray-900 dark:text-gray-100">3</div>';
      }
    } catch (error) {
      console.error('Error updating AI counters:', error);
    }
  }

  // Refresh AI News
  function refreshAINews() {
    const container = document.getElementById('ai-news-preview');
    if (container) {
      container.innerHTML = `
        <div class="text-center py-8">
          <div class="animate-spin w-8 h-8 border-3 border-emerald-500/20 border-t-emerald-500 rounded-full mx-auto mb-3"></div>
          <p class="text-gray-600 dark:text-gray-400 text-sm">Refreshing AI news...</p>
        </div>
      `;
      
      // Simulate refresh delay
      setTimeout(() => {
        loadAINews();
      }, 1000);
    }
  }

  // Make functions globally available
  (window as any).loadFeaturedSeries = loadFeaturedSeries;
  (window as any).loadHomeStats = loadHomeStats;
  (window as any).loadAIContent = loadAIContent;
  (window as any).refreshAINews = refreshAINews;

  (window as any).openCategoryModal = function(category, articles) {
    currentArticles = articles;
    const modal = document.getElementById('categoryModal');
    const modalContent = document.getElementById('modalContent');
    const modalTitle = document.getElementById('modalTitle');
    const modalCount = document.getElementById('modalCount');
    const modalIcon = document.getElementById('modalIcon');
    const searchInput = document.getElementById('articleSearch');

    // Icon mapping
    const icons = {
      'python': '🐍',
      'javascript': '🟨',
      'react': '⚛️',
      'django': '🎸',
      'database': '🗄️',
      'devops': '⚙️',
      'nginx': '🌐',
      'docker': '🐳',
      'system design': '🏗️',
      'programming': '💻'
    };

    // Color mapping
    const colors = {
      'python': 'from-blue-500 to-blue-600',
      'javascript': 'from-yellow-500 to-yellow-600',
      'react': 'from-cyan-500 to-cyan-600',
      'django': 'from-purple-500 to-purple-600',
      'database': 'from-purple-500 to-purple-600',
      'devops': 'from-orange-500 to-orange-600',
      'nginx': 'from-indigo-500 to-indigo-600',
      'docker': 'from-blue-500 to-blue-600',
      'system design': 'from-pink-500 to-pink-600',
      'programming': 'from-gray-500 to-gray-600'
    };

    // Update modal content
    modalTitle.textContent = category.charAt(0).toUpperCase() + category.slice(1);
    modalCount.textContent = `${articles.length} article${articles.length !== 1 ? 's' : ''}`;
    modalIcon.textContent = icons[category.toLowerCase()] || '📝';
    modalIcon.className = `w-10 h-10 bg-gradient-to-r ${colors[category.toLowerCase()] || 'from-gray-500 to-gray-600'} rounded-xl flex items-center justify-center text-white text-xl`;

    // Clear search
    (searchInput as HTMLInputElement).value = '';

    // Show modal with animation
    modal.classList.remove('hidden');
    setTimeout(() => {
      modal.classList.remove('opacity-0');
      modalContent.classList.remove('scale-95');
      modalContent.classList.add('scale-100');
    }, 10);

    // Render articles
    renderArticles(articles);

    // Prevent body scrolling
    document.body.style.overflow = 'hidden';
  };

  (window as any).closeCategoryModal = function() {
    const modal = document.getElementById('categoryModal');
    const modalContent = document.getElementById('modalContent');

    modal.classList.add('opacity-0');
    modalContent.classList.remove('scale-100');
    modalContent.classList.add('scale-95');

    setTimeout(() => {
      modal.classList.add('hidden');
      document.body.style.overflow = '';
    }, 300);
  };

  function renderArticles(articles) {
    const articlesList = document.getElementById('modalArticlesList');

    if (articles.length === 0) {
      articlesList.innerHTML = `
        <div class="p-8 text-center">
          <div class="text-gray-400 text-6xl mb-4">🔍</div>
          <h3 class="text-lg font-medium text-gray-900 dark:text-white mb-2">No articles found</h3>
          <p class="text-gray-600 dark:text-gray-400">Try adjusting your search terms.</p>
        </div>
      `;
      return;
    }

    articlesList.innerHTML = articles.map(article => `
      <a href="/blog/${article.slug}" class="block p-6 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors border-b border-gray-200 dark:border-gray-700 last:border-b-0">
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between">
          <div class="flex-1 min-w-0">
            <div class="flex items-center gap-2 mb-2">
              ${article.tags.slice(0, 3).map(tag => `
                <span class="px-2 py-1 bg-gray-100 dark:bg-gray-600 text-gray-600 dark:text-gray-300 rounded text-xs">
                  ${tag}
                </span>
              `).join('')}
            </div>
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2 hover:text-indigo-600 dark:hover:text-indigo-400 transition-colors">
              ${article.title}
            </h3>
            <p class="text-gray-600 dark:text-gray-400 text-sm line-clamp-2">
              ${article.description}
            </p>
          </div>
          <div class="flex items-center gap-4 mt-3 sm:mt-0 sm:ml-6 text-xs sm:text-sm text-gray-500 dark:text-gray-400">
            <span>${new Date(article.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}</span>
            <span>${article.readingTime}</span>
          </div>
        </div>
      </a>
    `).join('');
  }

  // Event delegation and initialization
  document.addEventListener('DOMContentLoaded', function() {
    // Load stats and featured series on page load
    loadHomeStats();
    loadFeaturedSeries();
    loadAIContent();

    // Handle category modal buttons
    document.addEventListener('click', function(e) {
      const target = e.target as HTMLElement;

      if (target.closest('.category-modal-btn')) {
        e.preventDefault();
        const btn = target.closest('.category-modal-btn') as HTMLElement;
        const category = btn.getAttribute('data-category');
        const articles = JSON.parse(btn.getAttribute('data-articles') || '[]');
        (window as any).openCategoryModal(category, articles);
      }

      // Handle close button
      if (target.closest('#closeModalBtn')) {
        (window as any).closeCategoryModal();
      }
    });

    // Search functionality
    const searchInput = document.getElementById('articleSearch');
    if (searchInput) {
      searchInput.addEventListener('input', function(e) {
        const query = (e.target as HTMLInputElement).value.toLowerCase();
        const filteredArticles = currentArticles.filter(article =>
          article.title.toLowerCase().includes(query) ||
          article.description.toLowerCase().includes(query) ||
          article.tags.some(tag => tag.toLowerCase().includes(query))
        );
        renderArticles(filteredArticles);
      });
    }

    // Close modal on escape key
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape') {
        (window as any).closeCategoryModal();
      }
    });

    // Close modal on backdrop click
    const modal = document.getElementById('categoryModal');
    if (modal) {
      modal.addEventListener('click', function(e) {
        if (e.target === this) {
          (window as any).closeCategoryModal();
        }
      });
    }
  });
</script>

<style>
  /* Enhanced animations */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(40px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  /* Series cards animation */
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(40px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  /* New fade up animation for topics */
  .animate-fade-up {
    animation: fadeUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-slide-up {
    animation: slideUp 0.6s ease-out forwards;
    opacity: 0;
  }

  .stats-grid > div {
    animation: fadeIn 0.6s ease-out forwards;
    animation-delay: calc(var(--animation-order, 0) * 0.1s);
    opacity: 0;
  }

  .stats-grid > div:nth-child(1) { --animation-order: 1; }
  .stats-grid > div:nth-child(2) { --animation-order: 2; }
  .stats-grid > div:nth-child(3) { --animation-order: 3; }
  .stats-grid > div:nth-child(4) { --animation-order: 4; }

  .projects-grid > div {
    animation: fadeIn 0.6s ease-out forwards;
    animation-delay: calc(var(--animation-order, 0) * 0.1s);
    opacity: 0;
  }

  .projects-grid > div:nth-child(1) { --animation-order: 1; }
  .projects-grid > div:nth-child(2) { --animation-order: 2; }
  .projects-grid > div:nth-child(3) { --animation-order: 3; }
  .projects-grid > div:nth-child(4) { --animation-order: 4; }
  .projects-grid > div:nth-child(5) { --animation-order: 5; }
  .projects-grid > div:nth-child(6) { --animation-order: 6; }

  .articles-list > div {
    animation: slideInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .articles-list > div:nth-child(1) { animation-delay: 0.1s; }
  .articles-list > div:nth-child(2) { animation-delay: 0.2s; }
  .articles-list > div:nth-child(3) { animation-delay: 0.3s; }
  .articles-list > div:nth-child(4) { animation-delay: 0.4s; }
  .articles-list > div:nth-child(5) { animation-delay: 0.5s; }

  /* Decorative floating animations */
  .float-1 {
    animation: float 6s ease-in-out infinite;
  }

  .float-2 {
    animation: float 8s ease-in-out infinite;
    animation-delay: -2s;
  }

  /* Enhanced hover effects for blog cards */
  .articles-list article:hover {
    transform: translateY(-8px) scale(1.02);
  }

  /* Line clamp utilities */
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }

  /* Custom scrollbar */
  ::-webkit-scrollbar {
    width: 8px;
  }

  ::-webkit-scrollbar-track {
    background: transparent;
  }

  ::-webkit-scrollbar-thumb {
    background: linear-gradient(to bottom, #8b5cf6, #3b82f6);
    border-radius: 4px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(to bottom, #7c3aed, #2563eb);
  }
</style>
