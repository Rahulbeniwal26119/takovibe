---
layout: ../../layouts/BlogPost.astro
title: "Python Code Generators (NamedTuple and dataclass)"
date: "2025-08-03"
author: "Rahul Beniwal"
description: "Learn how Python's NamedTuple and dataclass can simplify and optimize code with built-in code generation features, while understanding their differences and advanced tweaks."
image: "https://miro.medium.com/v2/resize:fit:1100/format:webp/1*EfEuN709Bkv3mt6KEGbXlA.png"
readingTime: "5 min read"
tags: ["python", "dataclass", "namedtuple", "code-generation", "software-development", "clean-code"]
type: "article"
canonical: "https://takovibe.com/blog/python-code-generators"
---

Python Code Generators (NamedTuple and dataclass)
Rahul Beniwal
Rahul Beniwal
4 min read · Apr 3, 2023

Code generators can help to write clean code faster if we define specifications correctly. Today lets us talk about two popular choices for code generation.

Zoom image will be displayed

Lets create a simple class

```python
class Point:

    def __init__(self, x:int, y:int):
        self.x=x
        self.y=y

    def __repr__(self):
        return self.__class__.__qualname__ + f"(x={self.x!r}, y={self.y!r})"

    def __eq__(self, other):
        if other.__class__ is self.__class__:
            return (self.x,self.y,)==(other.x,other.y,)
        return NotImplemented
```

So writing this same code with dataclass and NamedTuples

```python
# NamedTuple 
from typing import NamedTuple

class Point(NamedTuple):
    x: int
    y: int


# Dataclasses
from dataclasses import dataclass

@dataclass
class Point:
    x: int 
    y: int
```

Here code generators provide implementation of special dunder methods implicitly. Code generators also store class metadata and provide special methods.

### Dataclass

- `asdict` use for representing object properties to dict
- `astuple` use for representing object properties as tuple.
- `replace` use for creating new object from existing object by replacing one or more attributes.
- `__annotations__` provide information regarding the type annotation applied on attributes.
- `fields` use for accessing the descriptor which is use for internal representation of these attributes.

```python
from dataclasses import asdict
print("class_dict", asdict(c))

from dataclasses import astuple
print("class_tuple", astuple(c))

from dataclasses import replace
c2 = replace(c, x=3)
print("new_object", c2)

print("Annotations", c.__annotations__)

from dataclasses import fields
print(" ** Fields ** ")
class_fields = fields(c)
for field in class_fields:
    print(field)
```

### NamedTuple

- `_asdict` use for representing object attributes as dict.
- builtin tuple for representing object attributes as tuple.
- `_replace` use for creating new instance from existing.
- `__annotations__` provide information regarding the type annotation applied on attributes.

```python
print("class_dict", d._asdict())
print("class_tuple", tuple(d))
d2 = d._replace(x=3)
print("new_object", d2)
print("Annotations", d.__annotations__)
```

### Diff bw Dataclass and NamedTuple

1. NamedTuple can be unpack but dataclass cannot.
2. NamedTuple are immutable while dataclass are not.
3. NamedTuple is hashable while dataclass is not.
4. NamedTuple is C-binding based implementation while dataclass are pure Python.
5. NamedTuple are iterable while dataclass are not.

### Tweaking default dataclass

```python
from dataclasses import dataclass

@dataclass(order=True)
class Point:
    x: int 
    y: int = 10

p1 = Point(1,2)
p2 = Point(-1,2)

print(sorted([p1,p2]))
print(p1 == p2)
print(p1 > p2)
print(p1 < p2)
print(p1 >= p2)
print(p1 <= p2)
```

### Making dataclass immutable

```python
from dataclasses import dataclass, replace

@dataclass(order=True, frozen=True)
class Point:
    x: int 
    y: int = 10

p1 = Point(1,2)
```

### Using field for replacing default behavior

```python
from dataclasses import dataclass, field
from datetime import datetime

@dataclass(order=True, unsafe_hash=True)
class Employee:
    emp_id:int = field()
    name: str = field()
    gender: str = field()
    salary: int = field(hash=False, repr=False, metadata={'units': 'USD'})
    age: int = field(hash=False)
    viewed_by:list = field(default_factory=list, compare=False, repr=False)

    def access(self, access_id):
        self.viewed_by.append((access_id, datetime.now()))

e = Employee(1, "John", "M", 1000, 30)
f = Employee(2, "Jane", "F", 2000, 25)
e.access("Manager1")
e.access("Manager2")

f.access("Manager1")
f.access("Manager2")

result = sorted([e,f])
print(result)

print(e)
```

### Using Dataclass with Inheritance

```python
from dataclasses import dataclass, asdict

class Serialize:
    def __init__(self, kwargs):
        self.kwargs = kwargs
    
    def serialize(self):
        import json
        return json.dumps(self.kwargs)    

@dataclass
class Point(Serialize):
    x: int
    y: int

    def __post_init__(self):
        super().__init__(asdict(self))

c = Point(1,2)
print(c.serialize())
```

Yeah we got enough dose of code generators for start writing clean code.

Leave a 👏 if you like this
