---

---

<div
    id="top-banner"
    class="relative z-[100] w-full -translate-y-full border-b border-orange-500/20 bg-neutral-950 text-neutral-50 shadow-sm transition-transform duration-500 ease-out"
    style="display: none;"
>
    <div
        class="mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-2 sm:px-6"
    >
        <!-- Message -->
        <div class="flex min-w-0 items-center gap-3">
            <span
                class="flex h-6 w-6 shrink-0 items-center justify-center rounded-full border border-orange-400/30 bg-orange-400/10 text-orange-300"
                aria-hidden="true"
            >
                <svg
                    class="h-3.5 w-3.5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M12 3l1.8 5.2L19 10l-5.2 1.8L12 17l-1.8-5.2L5 10l5.2-1.8L12 3z"
                    ></path>
                </svg>
            </span>
            <p class="truncate text-sm font-medium">
                <span class="hidden sm:inline"
                    >ResumeVibe is open for early builders</span
                >
                <span class="sm:hidden">ResumeVibe is live</span>
                <span class="mx-2 hidden text-neutral-500 md:inline">/</span>
                <span class="hidden text-neutral-300 md:inline"
                    >Run AI diagnostics on your resume</span
                >
            </p>
        </div>

        <!-- Actions -->
        <div class="flex shrink-0 items-center gap-2">
            <a
                href="https://resumevibe.takovibe.com/"
                class="inline-flex items-center gap-1.5 rounded-full bg-orange-500 px-3 py-1.5 text-xs font-bold text-white transition-colors hover:bg-orange-600"
            >
                <span>Try ResumeVibe</span>
                <svg
                    class="h-3.5 w-3.5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M17 8l4 4m0 0l-4 4m4-4H3"
                    ></path>
                </svg>
            </a>

            <!-- Close -->
            <button
                id="close-banner"
                class="rounded-full p-1 text-neutral-400 transition-colors hover:bg-white/10 hover:text-white"
                aria-label="Dismiss"
            >
                <svg
                    class="w-4 h-4"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>
    </div>
</div>

<script>
    const BANNER_KEY = "takovibe_top_banner_closed_ts";
    const TWENTY_FOUR_HOURS = 24 * 60 * 60 * 1000;

    function initBanner() {
        const banner = document.getElementById("top-banner");
        const closeBtn = document.getElementById("close-banner");

        if (!banner) return;

        // Check Local Storage
        const lastClosed = localStorage.getItem(BANNER_KEY);
        if (lastClosed) {
            const timePassed = Date.now() - parseInt(lastClosed, 10);
            if (timePassed < TWENTY_FOUR_HOURS) {
                // Still within 24 hours, keeping hidden
                return;
            }
        }

        // Show Banner
        banner.style.display = "block";
        // Small delay for slide-down animation
        requestAnimationFrame(() => {
            banner.classList.remove("-translate-y-full");
        });

        // Handle Close
        closeBtn?.addEventListener("click", () => {
            banner.classList.add("-translate-y-full");

            // Record timestamp
            localStorage.setItem(BANNER_KEY, Date.now().toString());

            // Remove from DOM after transition
            setTimeout(() => {
                banner.style.display = "none";
            }, 500); // Match duration-500
        });
    }

    // Run on every page load (View Transitions compatible)
    document.addEventListener("astro:page-load", initBanner);
    document.addEventListener("DOMContentLoaded", initBanner);
</script>
